services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: flowdiffmap
      POSTGRES_USER: flowdiffmap
      POSTGRES_PASSWORD: flowdiffmap
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
